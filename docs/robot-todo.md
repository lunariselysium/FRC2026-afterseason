# Robot TODO

## Fault Visibility

- Add a central fault/status surface once the robot has a clear way to reflect faults to drivers.
- Make critical turret heading faults obvious with lights or another driver-visible signal after LEDs are installed.
- Consider audible/rumble feedback for critical mechanism faults if the driver station workflow supports it.
- Add homing timeout faults for intake deploy and turret pitch once faults can be reflected clearly.

## Controls

- Add driver-ready bindings for field-centric heading reset and slow/precision drive mode when controller layout has space.
- Decide whether drivetrain SysId bindings should be disabled behind a bring-up constant outside tuning sessions.
